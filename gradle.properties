# Force Gradle daemon to run on Java 25 so mc-26.1.2 (which requires Java 25) can be built.
# mc-1.21.11 still compiles to --release 21 via its build.gradle.
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-25.0.3.9-hotspot
org.gradle.jvmargs=-Xmx2G
